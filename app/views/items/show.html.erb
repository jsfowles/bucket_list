<h2><%= @item.title %></h2>
<% if @item.completed %>
	You completed this on <%= @item.completed_date %>!
<% else %>
	Goal to complete by: <%= @item.complete_by %>
	<%= render 'mark_complete', item: @item %>
<% end %>

<hr>

<%= link_to 'Delete this item', list_item_path(@list, @item), method: :delete %>
